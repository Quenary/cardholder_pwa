/**
 * Premitive types as strings.
 * Not quiet a 'types' because of 'date'.
 * It is used to sorting and filtering.
 */
export type PrimitiveStringType =
  | 'boolean'
  | 'string'
  | 'number'
  | 'date'
  | 'bigint';

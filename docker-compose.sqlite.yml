networks:
  cardholder_pwa:
    driver: bridge
services:
  app:
    image: ghcr.io/quenary/cardholder_pwa:1
    restart: unless-stopped
    volumes:
      - $HOME/.cardholder_pwa:/cardholder_pwa
    networks:
      - cardholder_pwa
    ports:
      - "80:80"